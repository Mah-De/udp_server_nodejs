{
  "name": "y",
  "version": "1.0.0",
  "description": "a simple app to receive websocket stream comes from esp32 mic_streamer_ws project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
